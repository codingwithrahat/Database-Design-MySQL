 create databse if not exists temp;

-- If the 'temp' database does not exist, it will be created
-- If it already exists, the statement is ignored
-- Without 'IF NOT EXISTS', an error occurs if 'temp' already exists
