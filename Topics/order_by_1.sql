select * from student 
order by age;

-- ORDER BY sorts the result set based on a column (age in this case).
-- By default, it sorts in ascending order (from smallest to largest).
-- You can also explicitly use ASC (ORDER BY age ASC) for ascending order.
