create table student2(
    id int,
    name varchar(50) unique, -- all stu name should be unqiue
    age int not null,
    primary key (id) -- primery key constraints added in id , another way to add constraints
    );
    
