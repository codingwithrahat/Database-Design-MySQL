select name from student where id = 1 or id = 2;

-- WHERE can also use logical operators like AND / OR.
-- Here, it checks if the student's id is either 1 or 2.
-- If the condition is true, the student's name will be returned.
