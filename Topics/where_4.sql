select name from student where age between 16 and 20;

-- BETWEEN checks if a value is within a range (inclusive)
-- This is the same as: age >= 16 AND age <= 20
-- Here, it returns the names of students whose age is between 16 and 20
