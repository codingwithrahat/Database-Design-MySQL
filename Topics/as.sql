SELECT id, name, salary as monthly_salary from instructor;

-- This query shows the ID, name, and salary columns from the instructor table.
-- The AS keyword creates a column alias: it renames 'salary' as 'monthly_salary'
-- only in the output, without changing the actual table structure.
