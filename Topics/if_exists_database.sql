drop database if exists temp;

-- Deletes the 'temp' database if it exists
-- If the database does not exist, the statement does nothing
-- Without 'IF EXISTS', an error occurs if 'temp' does not exist
