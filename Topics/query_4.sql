select name from student where age + 10 >= 26;

-- WHERE can also perform operations
-- Here, it checks if the student's age plus 10 is greater than or equal to 26
-- If the condition is true, the student's name will be returned
